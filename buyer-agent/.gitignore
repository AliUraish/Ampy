node_modules/
.env
uploads/*
!uploads/.gitkeep
data/sellerListings.json
# Runtime conversation state, same as sellerListings.json above — these are
# per-machine records of real messages sent and answers received, not source.
data/conversations.json
data/messageLog.json
*.log
.DS_Store
